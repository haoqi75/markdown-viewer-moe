{
  "name": "markdown-viewer-moe",
  "version": "1.9.3",
  "description": "由AI生成的Moe Markdown查看器",
  "scripts": {
    "build": "gulp build",
    "build:release": "gulp build && node script/release.js",
    "build:tools": "pnpm --filter moe-json-editor build:html && node script/copy-tools.js",
    "dev": "trap 'exit 0' EXIT; gulp clean build && http-server dist -p 8520 -a 127.0.0.1",
    "dev:tools": "pnpm --filter moe-json-editor dev",
    "start": "trap 'exit 0' EXIT; gulp clean build && http-server dist -p 8520 -a 127.0.0.1"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "clean-css": "^5.3.3",
    "del": "^7.1.0",
    "gulp": "^5.0.1",
    "gulp-clean-css": "^4.3.0",
    "gulp-concat": "^2.6.1",
    "gulp-htmlmin": "^5.0.1",
    "gulp-inline": "^0.1.3",
    "gulp-rename": "^2.1.0",
    "gulp-replace": "^1.1.4",
    "gulp-uglify": "^3.0.2",
    "http-server": "^14.1.1",
    "uglify-js": "^3.19.3"
  }
}